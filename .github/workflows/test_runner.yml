name: Test Cluster-CI Runner

on:
  push:
    branches:
      - main

jobs:
  test_pipeline:
    runs-on: self-hosted
    steps:
      - name: Trigger Orchestrator Script
        run: cluster-ci-run ${{ github.repository }} ${{ github.head_ref || github.ref_name }} ${{ secrets.GITHUB_TOKEN }}
